完成日: 2026-08-07
場所: `C:\AI\Antigravity\Projects\applications\Frame-Splitter`
公開: https://isajunsei.github.io/frame-splitter/
本体: `index.html` 単体（ビルド不要・画像は送信しない）

---

## 何をするツールか

複数画像を **余白なし** で1枚の PNG にまとめる。
ドロップ / ファイル選択 / Ctrl+V で追加。結果をダウンロードできる。

---

## 初期合成（自動レイアウト）

- 画像を段に分け、同じ段は高さをそろえ、各段の合計幅をキャンバス幅に合わせる
- **初期合成では縮小しない**（段の最大高に合わせ、小さい画像は拡大して幅をそろえる）
- 余白（隙間）は出さない
- 候補を採点して最大6案保持

```
点数 ＝ −（正方形からの遠さ × ASPECT_WEIGHT）−（拡大の大きさ × UPSCALE_WEIGHT）
```

定数（`index.html` 先頭）:
- `ASPECT_TARGET = 1.15`
- `ASPECT_WEIGHT = 1.00`
- `UPSCALE_WEIGHT = 1.5`
- `MAX_CANDIDATES = 6`
- `MAX_SIDE = 16000`

---

## 並び替えは2系統（別物）

### A. 「別の並びにする」ボタン

- 段分けの **別候補** に切り替える
- **縦横比・全体サイズが変わりうる**（縦長／横長になり得る）
- 押すと B の入れ替え状態はリセット

### B. 完成画像上のドラッグ＆ドロップ（本命の手動入れ替え）

- **いま表示中の縦横比・枠の形・全体ピクセルサイズは絶対に変えない**
- 枠（スロット）の位置・大きさは固定
- **任意の2枠の中身を入れ替える**（3枚でも4枚でも5枚でも同じ）
- 操作: 枠を掴んで別枠の上でドロップ
- 時計回りの自動ローテは **採用しない（削除済み）**

#### ドラッグ中の見た目

- エクスプローラー風の **小さいサムネ（ゴースト）** がカーソル右下に追従
- 元枠は半透明で「持ち上げた」感じ
- 掴み元: 青枠 / ドロップ先: 緑枠

#### 入れ替え後の描画（cover・余白なし）

- 各枠は必ず全面を埋める（市松の空きを出さない）
- アスペクト比は維持
- 足りない辺に合わせて拡大／縮小
- はみ出す辺は **中央基準で外側からカット**
- 未入れ替え（初期配置のまま）のときは従来どおり枠に引き伸ばし描画

#### リセットされるタイミング

- 画像の追加・削除・全部消す
- 「別の並びにする」
- 結果カードの白い余白クリック（全クリアして初期状態）

---

## UI メモ

- 完成画像: `cursor: grab` / ドラッグ中 `grabbing`
- 結果カードの画像以外（白い余白）クリック → 全クリア
- 下部固定: ダウンロード / 別の並びにする
- ドロップエリア高さ: `calc(100vh - 272px)`（`min-height: 280px`）

---

## 触るファイル

| ファイル | 役割 |
|---|---|
| `index.html` | 本体（UI + ロジック全部） |
| `README.md` | 公開向け説明 |
| `icons/` | ファビコン |
| `.github/workflows/pages.yml` | GitHub Pages |

---

## 実装の芯（変数）

- `layouts[]` … 自動レイアウト候補
- `layoutIdx` … 今の候補（A用）
- `slotMap[i]` … 枠 i に入っている「元 placed インデックス」（B用）
- `drawLayout(c, slotMap)` … 恒等なら stretch、入れ替え後は cover
- `drawCover` … 余白なし中央クロップ
- `#dragGhost` … ドラッグ中サムネ

---

## やらないこと（意図的）

- クリックで時計回りローテ（廃止）
- 入れ替え時に枠サイズ・キャンバス縦横比を変えること
- 入れ替え後に枠内へレターボックス（余白）を残すこと
